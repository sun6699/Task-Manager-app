Built a Task Manager app using Node.js, SQLite, and Docker, deployed online via Render with automated GitHub integration (CI/CD), allowing full CRUD operations on tasks.Built a Task Manager app using Node.js, SQLite, and Docker, deployed online via Render with automated GitHub integration (CI/CD), allowing full CRUD operations on tasks.
